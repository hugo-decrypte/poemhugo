<Window x:Class="PoemClient.View.TranslateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PoemClient.View"
        mc:Ignorable="d"
        Title="Translate Language" Width="500" SizeToContent="Height"
        Closing="TranslateClosing"
        Background="#F4F4F9"
        FontFamily="Segoe UI"
        FontSize="14">

    <Window.Resources>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="PathBox" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="#FFFFFF"
                                BorderBrush="#E0E0E0"
                                BorderThickness="1"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#90CAF9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="toggleButton"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border"
                                                Background="#FFFFFF"
                                                BorderBrush="#E0E0E0"
                                                BorderThickness="1"
                                                CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                                <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="#888888" VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#90CAF9"/>
                                                <Setter TargetName="border" Property="Background" Value="#F0F8FF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="12,0,30,0"
                                              VerticalAlignment="Center"
                                              IsHitTestVisible="False"/>
                            <Popup x:Name="PART_Popup"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                   MinWidth="{TemplateBinding ActualWidth}">
                                <Border Background="#FFFFFF"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,2,0,0">
                                    <ScrollViewer MaxHeight="200">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <StackPanel Margin="28">

        <!-- Modèle IA -->
        <ComboBox x:Name="AiModelSelector"
                  Style="{StaticResource RoundedComboBox}"
                  ItemsSource="{Binding LanguagesAI}"
                  Margin="0,0,0,20"
                  SelectionChanged="AISelectionChanged"
                  SelectedItem="{Binding SelectedAI, Mode=OneWay}"/>

        <Separator Background="#E0E0E0" Margin="0,0,0,20"/>

        <!-- Langues -->
        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="FromList"
                      Grid.Column="0"
                      Style="{StaticResource RoundedComboBox}"
                      ItemsSource="{Binding Languages}"
                      SelectionChanged="FromListSelectionChanged"
                      SelectedItem="{Binding SelectedFrom, Mode=OneWay}"/>

            <TextBlock Grid.Column="1"
                       Text="{Binding To}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Foreground="#555555"
                       Margin="16,0"/>

            <ComboBox x:Name="ToList"
                      Grid.Column="2"
                      Style="{StaticResource RoundedComboBox}"
                      ItemsSource="{Binding Languages}"
                      SelectionChanged="ToListSelectionChanged"
                      SelectedItem="{Binding SelectedTo, Mode=OneWay}"/>
        </Grid>

        <!-- Résultat -->
        <TextBlock Text="{Binding FilenameText}" Style="{StaticResource SectionLabel}"/>
        <Grid Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="36"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="Filename"
                    Grid.Column="0"
                    Style="{StaticResource PathBox}"
                    Click="BtnBrowseOutput_Click">
                <Button.Content>
                    <TextBlock Text="{Binding FilePath}" TextTrimming="CharacterEllipsis"/>
                </Button.Content>
            </Button>
            <Button Grid.Column="2"
                    Style="{StaticResource PathBox}"
                    Width="36"
                    ToolTip="Sélectionner un emplacement"
                    Click="BtnBrowseOutput_Click">
                <TextBlock Text="💾" FontSize="15"/>
            </Button>
        </Grid>

        <!-- Footer -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="TranslateButton"
                    Grid.Column="0"
                    Content="{Binding OkButton}"
                    Click="Translate"
                    Width="130" Height="36"
                    FontSize="13"
                    FontWeight="Normal"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="#F4F4F9"
                                BorderBrush="#CCCCCC"
                                BorderThickness="1"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#90CAF9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <TextBlock x:Name="Loading"
                       Grid.Column="1"
                       Text="{Binding loading}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Foreground="#AAAAAA"
                       FontSize="13"/>
        </Grid>

    </StackPanel>
</Window>