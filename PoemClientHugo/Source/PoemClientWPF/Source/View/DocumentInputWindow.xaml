<Window x:Class="PoemClient.View.DocumentInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DocumentReader}" Width="580" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Closing="Window_Closing" Background="#F4F4F9" FontSize="14" FontFamily="Segoe UI">

    <Window.Resources>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="PathBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#222222"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                       <Border x:Name="border"
                            Background="#F4F4F9"
                            BorderBrush="#CCCCCC"
                            BorderThickness="1"
                            CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#90CAF9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="toggleButton"
                                        Background="#FFFFFF"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                        Focusable="False">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border"
                                                Background="#FFFFFF"
                                                BorderBrush="#E0E0E0"
                                                BorderThickness="1"
                                                CornerRadius="6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                                <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z"
                                                    Fill="#888888" VerticalAlignment="Center"
                                                    HorizontalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#90CAF9"/>
                                                <Setter TargetName="border" Property="Background" Value="#F0F8FF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            Margin="12,0,30,0"
                                            VerticalAlignment="Center"
                                            IsHitTestVisible="False"/>
                            <Popup x:Name="PART_Popup"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Placement="Bottom"
                                PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                MinWidth="{TemplateBinding ActualWidth}">
                                <Border Background="#FFFFFF"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,2,0,0">
                                    <ScrollViewer MaxHeight="200">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <StackPanel Margin="28">

        <!-- Modèle IA (sans titre) -->
        <ComboBox x:Name="AiModelSelector"
                  Style="{StaticResource RoundedComboBox}"
                  ItemsSource="{Binding LanguagesAI}"
                  SelectionChanged="AISelectionChanged"
                  SelectedItem="{Binding SelectedFrom, Mode=OneWay}"
                  Margin="0,0,0,0"/>

        <Separator x:Name="AiSeparator" Background="#E0E0E0" Margin="0,20,0,20"/>
        <!-- Format + Document + Résultat dans un Grid commun pour alignement -->
        <Grid Margin="0,0,0,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="36"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="36"/>
            </Grid.ColumnDefinitions>

            <!-- Format -->
            <TextBlock Grid.Row="0" Grid.ColumnSpan="5"
                       Text="{Binding Format}" Style="{StaticResource SectionLabel}" Margin="15,0,0,6"/>
            <TextBox x:Name="txtFormatFilePath" Grid.Row="2" Grid.Column="0"
                     Style="{StaticResource PathBox}"
                     Text="{Binding PathFormatFile, Mode=OneWay}"/>
            <Button Grid.Row="2" Grid.Column="2"
                    Style="{StaticResource IconButton}"
                    ToolTip="{Binding File}"
                    Click="BtnBrowseFormatFile_Click">
                <TextBlock Text="📄" FontSize="15"/>
            </Button>

            <!-- Document -->
            <TextBlock Grid.Row="3" Grid.ColumnSpan="5"
                       Text="{Binding ToAnalyse}" Style="{StaticResource SectionLabel}" Margin="15,16,0,6" Grid.RowSpan="2"/>
            <TextBox x:Name="txtFilePath" Grid.Row="6" Grid.Column="0"
                     Style="{StaticResource PathBox}"
                     Text="{Binding PathFile, Mode=OneWay}"/>
            <Button Grid.Row="6" Grid.Column="2"
                    Style="{StaticResource IconButton}"
                    ToolTip="{Binding File}"
                    Click="BtnBrowseInputFile_Click">
                <TextBlock Text="📄" FontSize="15"/>
            </Button>
            <Button Grid.Row="6" Grid.Column="4"
                    Style="{StaticResource IconButton}"
                    ToolTip="{Binding Folder}"
                    Click="BtnBrowseInputFolder_Click" Margin="1,0,-1,16" Grid.RowSpan="2">
                <TextBlock Text="📁" FontSize="15"/>
            </Button>

            <!-- Résultat -->
            <TextBlock Grid.Row="8" Grid.ColumnSpan="5"
                       Text="{Binding ToSave}" Style="{StaticResource SectionLabel}" Margin="15,0,0,6"/>
            <TextBox x:Name="txtOutputPath" Grid.Row="10" Grid.Column="0"
                     Style="{StaticResource PathBox}"
                     Text="{Binding PathSave, Mode=OneWay}"/>
            <Button Grid.Row="10" Grid.Column="2"
                    Style="{StaticResource IconButton}"
                    ToolTip="{Binding Save}"
                    Click="BtnBrowseOutput_Click">
                <TextBlock Text="💾" FontSize="15"/>
            </Button>
        </Grid>

        <!-- Footer -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnAnalyze" Grid.Column="0"
                    Content="{Binding AnalyseSave}"
                    Width="130" Height="36"
                    Click="BtnAnalyze_Click"
                    FontSize="13"
                    FontWeight="Normal"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="#F4F4F9"
                                BorderBrush="#CCCCCC"
                                BorderThickness="1"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#90CAF9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <TextBlock x:Name="Loading" Grid.Column="1" Text=""
                       VerticalAlignment="Center" HorizontalAlignment="Right"
                       Foreground="#AAAAAA" FontSize="14"/>
        </Grid>

    </StackPanel>
</Window>