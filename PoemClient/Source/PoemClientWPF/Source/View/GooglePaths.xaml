<Window x:Class="PoemClient.Source.View.GooglePaths"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" 
      Title="GooglePaths" Height="150" Width="550" MinWidth="550" MinHeight="150" Closing="Window_closing">
    <Window.Resources>
        <Style x:Key="ToggleSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="15"/>
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    <Grid Background="White" Name="contentGrid">
        <!--Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="{Binding lbl_vehicule}" Grid.Row="2" Grid.Column="1" Margin="0 10 0 0" Name="label_vehicule" FontSize="14"/>
            <ComboBox SelectionChanged="ComboboxVehiculeSelectionChanged" Width="Auto" Grid.Column="2" Grid.Row="2" Margin="0 10 0 0" Name="combobox_vehicule" MaxDropDownHeight="150"/>

            <Label Content="{Binding lbl_tour}" Grid.Row="3" Grid.Column="1" Margin="0 10 0 0" Name="label_tour" FontSize="14" />
            <ComboBox Width="Auto" Grid.Row="3" Grid.Column="2" Margin="0 10 0 0" Name="combobox_tour" MaxDropDownHeight="150"/>

            <Button Grid.Row="3" Grid.Column="3" FontWeight="Normal" FontFamily="Segoe UI" FontSize="12" Content="{Binding btn_itinerary}" Margin="0 10 0 0" HorizontalAlignment="Center" HorizontalContentAlignment="Left" Height="Auto" Width="100" Name="btn_tour" Click="OnClickSearch" Background="White" Padding="5" BorderThickness="1" />
        </Grid-->
    </Grid>
</Window>
